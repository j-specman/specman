## Coding conventions

- Always used braces and line breaks for if and else blocks, even if they are not strictly necessary, e.g. because the blocks consists only of one line. This improves readability and reduces the chance of errors when modifying the code in the future.
- Do not use Gotos.
